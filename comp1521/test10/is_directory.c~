#include<stdio.h>
#include <unistd.h>

int main(int argc, char **argv){
    if(argc == 1) printf("0\n");
    else {
        FILE *x = fopen(argv[1],"r");
        if(x == NULL) printf("0\n");
        else printf("1\n");
    }
}
